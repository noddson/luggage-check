{
  "name": "luggage-check",
  "version": "0.1.0",
  "private": true,
  "description": "A tool for estimating and visualizing luggage fit in common European rental cars.",
  "type": "module",
  "scripts": {
    "validate:configs": "node scripts/validate-configs.mjs",
    "check": "npm run validate:configs && npm run smoke:app",
    "start": "node scripts/serve-app.mjs",
    "smoke:app": "node scripts/smoke-app.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
